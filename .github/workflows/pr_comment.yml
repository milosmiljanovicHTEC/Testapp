name: example
on: pull_request
jobs:
  example:
    name: example
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: mkdir -p output/
      - run: curl -s http://milosmi9:11770767fff910d8128d4ee40b0588d4b0@jenkinstestnow.ddns.net/job/testerosa/lastBuild/consoleText |sed -n '9'p > output/output.txt 
      - uses: actions/upload-artifact@v1
        with:
          name: output  
          path: output
      - uses: actions/download-artifact@v1
        with:
          name: output
      - name: comment PR
        uses: machine-learning-apps/pr-comment@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          path: output/output.txt
